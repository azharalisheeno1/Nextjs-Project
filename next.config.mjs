/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
      domains: ['www.insidewink.com', 'images.unsplash.com', 'plus.unsplash.com'],
      },
};

export default nextConfig;
